# Repository workflow

- Put every change on a `codex/` branch and open a GitHub pull request.
- Never merge a pull request or push changes directly to `main`.
- Leave the pull request for Ali to read and approve before any merge.
